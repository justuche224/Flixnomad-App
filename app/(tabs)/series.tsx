import { StyleSheet, Text, View } from "react-native";

const Series = () => {
    return (
        <View>
            <Text>Series</Text>
        </View>
    );
};

export default Series;

const styles = StyleSheet.create({});
